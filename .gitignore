# Rust
/target
crates/*/target
Cargo.lock.orig

# Flatpak
/build-dir
/.flatpak-builder
/repo
/builddir-flatpak.log
/.flatpak-builder-*

# Flatpak release flow — build outputs, not sources.
/repo-release
/build-dir-release
/Muybridge.flatpak

# Meson
/_build
/builddir

# Editor / OS
*.swp
*~
.DS_Store

# Screenshots dropped in while diagnosing something on screen. Useful in the
# moment, not part of the source — AppStream screenshots live in data/.
/Troubleshooting

# Scratch tree used by build-aux/publish-repo.sh (must sit on the same
# filesystem as the flatpak-builder state dir, so it cannot live in /tmp).
/.publish-tmp.*
