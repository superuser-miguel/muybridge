[package]
name = "muybridge"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "GTK4/libadwaita frontend for extracting still frames from video with ffmpeg"

[[bin]]
name = "muybridge"
path = "src/main.rs"

[dependencies]
# Imported as `gtk` / `adw` (idiomatic gtk-rs). gtk re-exports gio & glib.
gtk = { package = "gtk4", version = "0.11", features = ["v4_12"] }  # FileDialog 4.10, CssProvider::load_from_string 4.12
adw = { package = "libadwaita", version = "0.9", features = ["v1_6"] }  # SpinRow/SwitchRow 1.4, AboutDialog 1.5, accent_color_rgba 1.6
ffmpeg-frames = { path = "../ffmpeg-frames" }
