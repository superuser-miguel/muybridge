[package]
name = "ffmpeg-frames"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "UI-free engine: ffmpeg/ffprobe argv construction and output parsing for frame extraction"

[dependencies]
